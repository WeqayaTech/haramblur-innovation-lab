# ── Stage 1: describe.py (VLM) ──────────────────────────────────────────
# Qwen2.5-VL needs a recent transformers. Torch should match the pod's CUDA.
torch>=2.1
transformers>=4.49
qwen-vl-utils>=0.0.8
accelerate>=0.30

# ── Stage 2: cluster.py ─────────────────────────────────────────────────
sentence-transformers>=2.7
scikit-learn>=1.3
hdbscan>=0.8.33      # falls back to KMeans if missing
umap-learn>=0.5.5    # falls back to PCA if missing

# ── Shared ──────────────────────────────────────────────────────────────
opencv-python>=4.8
PyYAML>=6.0
Pillow>=10.0
matplotlib>=3.7
numpy>=1.24

# ── EXP-2026-06: API describers (api_describers.py) ─────────────────────
# Only needed for the engine(s) you actually select via describe.py --engine.
# describe.py auto-loads .env for the API keys.
python-dotenv>=1.0
# openai>=1.0
# google-genai>=0.3
# anthropic>=0.40
