# Spotlight labeling pipeline — install on every pod before running.
# This is the file pod_setup.sh installs. Run BEFORE the torch step.
#
#   pip install -r requirements_pipeline.txt
#
# Covers: autolabel_sam_raw.py, spotlight_run.py, spotlight_batch.py,
#         parallel_emit.py, verify_labels.py, estimate_cost.py, dump_crops.py,
#         and the crowd-scene track (crowd_sam_masks/classify/trace).
#
# torch is NOT listed — pod_setup.sh installs a CUDA-matched version
# separately (cu124 for Ada/Ampere, cu128 for Blackwell).

transformers>=4.45
accelerate>=0.30
opencv-python>=4.8
Pillow>=10.0
PyYAML>=6.0
numpy>=1.24
tqdm>=4.60
python-dotenv>=1.0
