# secrets
.env

# local runtime state
.serve.pid
.claude/settings.local.json
.claude/scheduled_tasks.lock

# local-only volume mount / mirrors of pod output (reproducible from the pod, not source)
workspace/
_*_review/
deliverables/
models/
runpod_volume/__pycache__/

# archives and model weight binaries
*.zip
*.tar.gz
*.pt
*.pth
*.ckpt
*.onnx
*.tflite
*.safetensors
*.h5

# generated report exports — the .md write-up is the source of truth
*.pdf
*.docx
*.html

# PNG copies of SVG charts (generated for ClickUp pasting, regenerate from the SVGs)
experiments/assets/png/

# python / OS junk
__pycache__/
*.pyc
.ruff_cache/
.DS_Store
