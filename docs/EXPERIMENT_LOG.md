# Experiment log — every EXP, one row, sorted by date

**The index.** For the full story of any row, open its doc under `experiments/EXP-YYYY-NN-*.md`
(each has the pre-registered bar, method, and a "verify it yourself" appendix). This file exists
so "what's been tried" is a table scan, not a re-read of CLAUDE.md's history.

**When you close an experiment, add its row here in the same session** — this is the one file
that must never fall behind (EXP-2026-18 went undocumented in CLAUDE.md for weeks; that's the
failure mode this file exists to prevent).

Component tags refer to `docs/COMPONENT_FRAMEWORK.md`: **C1** detection, **C2** age (adult vs
child), **C3** gender.

| EXP | Date | Component | Question | Verdict | Status |
|---|---|---|---|---|---|
| [01](../experiments/EXP-2026-01-qwen-label-verification.md) | pre-07-13 | C2, C3 | Can Qwen2.5-VL-7B verify gender+age labels? (vs LAGENDA) | Gender ~99.3%, child recall 99.6%, but teens misread as children 68% of the time. Gulf-dress not tested. | Closed — informed the verifier-gate idea |
| [02](../experiments/EXP-2026-02-sam-autolabeler-accuracy.md) | pre-07-13 | C1, C2, C3 | How accurate is the production SAM3 auto-labeler itself, same LAGENDA sample? | Detection 98.7% recall; 3-class 88.7% (between the 80/90 bars); 93% of error is adult→Child leak, not gender confusion. Cross-class NMS leak 0.7% (unconfirmed). | Closed — pod re-run for confirmation still pending |
| [03](../experiments/EXP-2026-03-sam-negatives-and-crowds.md) | 2026-07-13 | C1 | Detection on person-free scenes and exhaustively-labeled crowds (LAGENDA structurally can't test either) | Objects (dolls/statues): 62.2% of images gain ≥1 false person. PASS (empty scenes): 8.4 FP/100 imgs. CrowdHuman: precision 86.6% ✅ but recall 77% ❌ (misses 1-in-4 in occlusion). Overturned "detection is solved." | Closed — overlay adjudications still pending |
| [04](../experiments/EXP-2026-04-sam-person-prompt-detection.md) | 2026-07-14 | C1 | Does a single `"person"` prompt beat the 3-class gendered prompt for detection only? | **Rejected.** PASS FPs nearly tripled (8.4→27.4/100), objects worse, crowd recall gain under the adoption bar. Gendered prompts were an accidental specificity filter. | Closed |
| — | — | C2 | The age-component experiment (MiVOLO V2 on the same LAGENDA sample) | — | **Never run** — still an open candidate, see CLAUDE.md "What's next" |
| [06](../experiments/EXP-2026-06-vlm-cost-accuracy-comparison.md) | 2026-07-16→23 | C1, C2, C3 | Commercial VLMs (GPT-5.6 Sol, Gemini 3.5 Flash/Pro/3.6, Claude Sonnet 5, Qwen3.7-Plus) as whole-image auto-labelers | Smoke test only (n=3-250/dataset). SAM3 keeps crowd-recall lead; Gemini tight-but-blind, Sol/Qwen see-more-worse-box. Surfaced the box-convention census (Gemini y-first, Qwen x-first) and made **Gemini 3.5 Flash-Lite** the verifier-gate default candidate. | Closed |
| [07](../experiments/EXP-2026-07-sam-conf-fp-gate.md) | 2026-07-19 | C1 | Can SAM3's own detection confidence gate out its false positives? | **Rejected.** FP confidence (0.705 mean) overlaps real-detection confidence (0.735); most-confident FP class is "Woman." Confidence measures form-match, not real-person-ness. | Closed — decisive |
| [08](../experiments/EXP-2026-08-lite-verifier-gate.md) | concl. 2026-07-22 | C1 | Does Gemini 3.5 Flash-Lite work as an independent existence-verifier gate on SAM3's detections? | **Validated.** TP-keep 98.9% (bar 93%), FP-kill ~70%+ (PASS "survivors" were mostly real people on adjudication). Cost $0.54/1k detections. **Lite takes the gate seat.** | Closed |
| [09](../experiments/EXP-2026-09-sam3-vs-flashlite-crowd.md) | 2026-07-23 | C1, C3 | Head-to-head: SAM3 vs Flash-Lite as the *detector*, same 100 crowd images | SAM3 keeps the detector seat decisively (recall 78.2% vs 49.2%; Lite adds ~nothing as a second detector, +1.6 pts union). Lite precision/box-tightness better but under the win bar. | Closed |
| [10](../experiments/EXP-2026-10-pipeline-v1-mask-highlight.md) | 2026-07-23→26 | C1, C2, C3 | End-to-end validation of the assembled "Spotlight" pipeline (SAM3 detect → mask-highlight crop → Flash-Lite verdict → merge/emit) | **Passed after one schema iteration** (mask tint was hiding small people; fixed with outline+upscale). Final: TP-keep 99.34%/98.48%, gender 99.05%, adult(≥18)→Child leak 0%, child recall 32/32. | **Closed — became the production pipeline** |
| 11 | run 2026-08-03 | temporal | First measurement of the shipped model's frame-to-frame (video) behavior | Flicker is large (114–921 blur toggles/min). 90–99% of dropouts are threshold gaps, not real misses; symmetric Woman↔Man oscillation on borderline people. Class flips (the consequential axis) are near-zero. | Closed — cure (temporal policy) not yet built |
| [12](../experiments/EXP-2026-12-yolo26-production-candidate.md) | 2026-08-04→05 | C1, C2, C3 | Should the Spotlight retrain target YOLO26 instead of the production YOLO-MIT v9 architecture? | yolo26n fine-tune beats production YOLO-MIT on every product-pain metric (objects 53.7%→12.7%, gender 87.2%→90.7%, leak 0.77%→0.11%) but **loses small-person/crowd recall** (39.6%→32.8%) — traced to Spotlight's unknown-dropped-as-background label choice. | Closed — became the new training lineage |
| [13](../experiments/EXP-2026-13-yoloe-promptable-candidate.md) | 2026-08-05→06 | C1, C2, C3 | Zero-shot YOLOE (open-vocab prompts) as a labeler/detector, plus a free distractor-vocab FP-suppression trick | **Rejected for every seat** — no confidence threshold makes zero-shot recall and gender both acceptable simultaneously. Distractor-vocab trick only half-clears its bar; shelved but validated-safe. | Closed |
| [14](../experiments/EXP-2026-14-unknown-gradient-suppression.md) | trained 08-06/07, eval 08-08 | C1, C2, C3 | Does class-gradient suppression (unknown-gender boxes supervise geometry, not class) recover recall lost to background-masking, on YOLO26n? | +3.7 crowd recall, objects FP *improved* too, gender best-measured (91.1%). Formally missed the +5 bar (+3.7) but the mechanism works and became the new baseline. | Closed — **became `y26n_gradsupp`, the baseline lineage** |
| [15](../experiments/EXP-2026-15-yoloe-finetune-notperson.md) | trained 08-07, closed 08-08 | C1, C2, C3 | Port gradient-suppression to a YOLOE fine-tune | Ties `y26n_gradsupp` on every metric at 45% more compute (49.1 vs 33.8ms). | **Closed — YOLOE line closed for good** |
| [16](../experiments/EXP-2026-16-yolo26-non-e2e-head.md) | run complete 08-12 | C1 | Train YOLO26n *without* the one-to-one (NMS-free) head — does the dense head do better untrained-around? | +0.3/+0.2 mAP vs the ≥0.5 bar → **not adopted**. But found `export(end2end=False)` gives the same deployment format for free from any existing checkpoint, no retrain needed. | Closed |
| [17](../experiments/EXP-2026-17-two-axis-head.md) | trained 08-16/17, stopped ep.66/80 | C2, C3 | Separate gender and age into two independent label axes (with explicit Unknown on each) instead of one 3-class label | Mechanism works (verified 4 ways) but fails 4/6 no-regression bars — recall and FP moved together (looser operating point). Confirms Woman→Man asymmetry (2.7:1) reproduces inside a trained model. | **Not adopted** — matched-FP re-sweep never run, would be step 1 if revisited |
| [18](../experiments/EXP-2026-18-warm-restart-musgd.md) | run complete 08-17 | C1 | Does 50 more epochs at YOLO26's own MuSGD optimizer (not AdamW, which every prior run silently used) fix the pure one-to-many model? | Self-improvement bar cleared, but deployment bar not (object-set FP regression vs current best). `y26n_gs_nms` stays the recommendation. | Closed |
| [19](../experiments/EXP-2026-19-small-person-benchmark.md) | built 08-20, extended through 08-28 | C1, C2, C3 | How do models perform on SMALL people specifically? (no benchmark we owned could isolate this before) | Production model is **worst** small-person detector (5.11% recall) despite fewest FPs. Ranking flips by arm: gelan/r4fw wins real crowd photos, `y26n_sop50` wins portraits/avatars. Points at Spotlight's Woman→Man label asymmetry, not architecture. | Closed — informed the humanshaped/smallpatch training line |
| [20](../experiments/EXP-2026-20-yolo26s-humanshaped-smallpatch.md) | trained 2026-09-05→08 | C1, C2, C3 | Relabel human-shaped toys/statues as real targets (age-overrides-gender promotion) + step up to YOLO26s + small-object-paste augmentation | **Best model on every mAP/holdout/crowd-recall number measured so far** (holdout mAP50 0.928, best on every exposure tier). object_set FP not comparable across the policy change. | **Closed — current recommended model** |

## Non-numbered milestones (production runs / dataset builds, not single-question experiments)

These don't fit the "one question, one bar" EXP format but are equally load-bearing. Detail
lives in `docs/DATASET_REGISTRY.md` (datasets) and `docs/MODEL_COMPARISON.md` (models); this row
just anchors the date.

| Date | What | One-line result | Detail |
|---|---|---|---|
| 2026-07-28→30 | **Production run:** Spotlight labels the full OIV7 train split | 475,207 images → 1.58M people, 100%-verified, $507 | `docs/SPOTLIGHT_PRODUCTION_RUN.md` |
| 2026-08-08 | Five/seven-model benchmark round (gradsupp validated, latency session) | `y26n_gradsupp` non-e2e read is the free +1.9pt win; recommendation set | `experiments/ROUND-2026-08-08-TEAM-BRIEF.md` |
| 2026-08-10 | LAGENDA v2 rebuild + mAP protocol overhaul (pycocotools-conventional) | Corrected LAGENDA detection recall 72-80% (was prominent-subject-only ~93%) | `docs/MODEL_COMPARISON.md` |
| 2026-08-18 | **`haramblur_holdout` built** — first dedicated eval set + first Gulf-dress slice | Gulf-dress bias quantified: SAM3 misreads 4.94% of shaykhs as Woman; Gemini gate fixes to 0% | `docs/DATASET_REGISTRY.md`, `docs/HOLDOUT_BENCHMARK_HANDOFF.md` |
| 2026-08-23 | LAGENDA `fl1199` built — the only true 3-way (Gemini vs Sol vs human) set | 1,199 images, 1,514 fully human-labelled people | `docs/LAGENDA_HANDOFF.md` |
| 2026-08-25→29 | `deploy_compare` ship-table, five-model round, small-person QA slice | y26n_warm50 recommended deployable threshold; sheikh-context-women exposure gap found | memory `deploy-compare-ship-table` |
| 2026-08-31 | `not_person` hard-negative mining from production Spotlight verdicts | 9,254 images where SAM3+Gemini disagreed on person-hood — untested training signal | `docs/DATASET_REGISTRY.md` |
| 2026-09-05→08 | Humanshaped labeling policy change + benchmark | See EXP-20 above | `docs/MODEL_COMPARISON.md` |
| 2026-09-08 | TFLite fp32/INT8 export + latency bench + threshold sweeps (CrowdHuman/PASS detection, LAGENDA classification IoU>=0.5 @ 0.05 increments), `y26n_humanshaped_v2` vs `y26n_noe2e_warm50-2` (export-decision pass; exp20's two newest nano runs ruled out — stalled epoch 2-3/100 when their pod died) | INT8 is a 3.4x size win everywhere but only a speed win at 320px (slower than fp32 at 640px); the two checkpoints are statistically indistinguishable on both the recall/precision/FP curve and the per-class LAGENDA classification sweep; acc3's rise at high thresholds is a survivorship effect, not a real accuracy gain | `docs/MODEL_COMPARISON.md`, `models/humanshaped_v2_20260902/`, `models/noe2e_warm50-2_20260908/`, `models/threshold_sweep_20260908/` |
| 2026-09-09 | Investigated why `y26n_humanshaped_v2` scored ~10 mAP50 points higher than `y26n_noe2e_warm50-2` on LAGENDA — re-scored all 13 models on a matched, corrected 4,601-image LAGENDA set (`map_eval.py`, no re-inference) | **Bug, not a real effect**: every model except the two humanshaped ones had been scored on a stale 4,899-image LAGENDA dump (298 images from before a known OIV7-train-contamination fix); once matched, `y26n_humanshaped_v2` and `y26n_noe2e_warm50-2` land within 0.2 mAP50 pts of each other, and 5 other models now outscore both humanshaped checkpoints on LAGENDA. Retracts the "humanshaped wins LAGENDA" claim in EXP-20 and the 2026-09-05/08 section of `docs/MODEL_COMPARISON.md`; every other benchmark in both is unaffected | `docs/MODEL_COMPARISON.md` → "LAGENDA benchmark-set mismatch found + corrected", `experiments/EXP-2026-20-yolo26s-humanshaped-smallpatch.md` |
| 2026-09-09 | **INT8 quantization accuracy matrix, train-calibrated** — 3 checkpoints (`y26n_humanshaped_v2`, `y26n_noe2e_warm50-2`, `y26s_humanshaped_smallpatch_v1`) × {640, 416, 320} × {fp32 `.pt`, fp32 TFLite, INT8 TFLite}, 27 rows on Spotlight-val; redoes the 2026-09-08 INT8 pass, which had calibrated on the eval split itself (4,232 images) — this run calibrates on a fixed 500-image train subset (`/workspace/exp12/calib_train500.txt`, sha256 `55d0a78e…bff9`, `split=train`) | TFLite export is faithful (fp32 TFLite = fp32 `.pt` within ±0.005 in all 9 cells). INT8 costs localization, not IoU-0.5 detection: mAP50-95 −4.2 / −4.7 / −6.6 pts at 640, −1.1 / −2.6 / −5.1 at 416, ~flat at 320; mAP50 flat-to-up (nanos +0.5…+3.0, y26s −0.4 / −1.1 / +1.3). y26s is the *least* INT8-robust. Child AP50 rises +3.7…+5.3 for the nanos under INT8 — unexplained, needs the LAGENDA classification sweep on the INT8 files. 500 train-image calibration ≥ 4,232 val-image calibration in 7/8 cells (val arm retired). The old val-calibrated y26s@640 "int8" was an unquantized 38 MB file (score = fp32 TFLite exactly). Spotlight-val-only: valid for within-model precision deltas, not cross-model ranking | `docs/MODEL_COMPARISON.md` → "INT8 quantization accuracy matrix — train-calibrated, corrected (2026-09-09)", `models/quant_matrix_calib500_20260909/` (28 `_map.json`), pod `/workspace/quant_matrix_calib500/`, `/workspace/exports/*_calib500/` |
| 2026-09-10 | **FP16 TFLite arm** — the 9 train-calibrated fp32 TFLite exports weight-cast to float16 via `ai_edge_quantizer` `float_casting` (ultralytics 8.4.146 refuses `quantize=16` on its litert path); scored on Spotlight-val with the same pipeline as the INT8 matrix, plus an INT8-vs-fp32 disagreement gallery for `y26n_humanshaped_v2` @640 at conf 0.45 | FP16 = fp32 within ±0.0004 mAP in all 9 cells at exactly half the file size (5.1 MB nano / 19.3 MB y26s) — the zero-risk half-size option and the safe choice at 640 where INT8 loses 4-7 pts mAP50-95; not a CPU speed win. Gallery (both nanos, corrected 2026-09-10 after an owner review caught double-counting): 85% of images identical; INT8 boxes ≥0.05 IoU looser on ~37% of shared people; the apparent "+19% FPs" was mostly a 7x jump in same-class duplicate boxes on already-found people (23→159 hv2, 25→153 noe2e — NMS no longer merges the quantized twin), true boxes-on-nothing moved only 219→241 (hv2) and 182→152 (noe2e); ~670 "FPs" per arm are 2nd boxes of another class on a labelled person (per-class NMS), not phantoms; class flips 376 (hv2, ~50/50 who is right) / 264 (noe2e, fp32 right 61%), Woman→Child the largest bucket in both | `docs/MODEL_COMPARISON.md` (FP16 subsection), `docs/DEPLOYMENT_MODELS_AND_PATHS.md`, `models/quant_matrix_calib500_20260909/`, Drive package `models/haramblur_models_20260909/`, gallery artifact dce04dfb |
| 2026-09-10 | **TF.js builds for local extension trials** — `y26n_noe2e_warm50-2` and `y26s_humanshaped_smallpatch_v1` exported to TF.js graph models at 640/416/320, float + `--quantize_uint8` (`vlm-cluster/tfjs_build_two.sh`, pinned `ultralytics==8.4.82`, `end2end=False`), joining the 2026-09-02 `y26n_humanshaped_v2` set; contract checked against the shipped `src/models/<id>/` folders (`images [1,S,S,3]` → `output_0 [1,7,N]`) | Not an accuracy experiment — a packaging step. The extension is TF.js and cannot load the benchmarked `.tflite` INT8 files; `uint8` here is weight-only (activations float32), so the static-INT8 mAP losses do not apply and the fp32 rows do. Sizes: nano 2.97–3.00 MB uint8 / 10.1–10.2 MB float; y26s 10.3–10.4 / 38.7–38.9 MB (shipped `v11nclean2` tier = 34 MB) | `models/extension_tfjs_20260910/` (18 folders + README with tier ids, IndexedDB-cache caveat, SHA256SUMS), `docs/DEPLOYMENT_MODELS_AND_PATHS.md` TF.js table, pod `/workspace/exports/tfjs_all/` |
| 2026-09-11 | **EXP-2026-21 — why INT8 loses at 640** — pre-registered ablations on pod `superior_amethyst_goose`: A box-edge error analysis on existing dumps, B `w8a16`, C `w8a32`, D W8A8 with the detect head's decode ops (D1) / whole head (D2) left float via `ai_edge_quantizer` `NO_QUANTIZE`; all @640 on Spotlight-val with the 2026-09-09 scorer | Loss is int8 activation resolution in the head decode ops (multi-scale regression concatenated onto one 256-level scale: 2.5/5/10 px steps; edge error ∝ stride: 1.9/3.6/4.8 px). Weights cost ~0.5 pt (`w8a32` −0.6/−0.7/−0.4); `w8a16` recovers equally but runs ~7× slower. **D1 removes the loss entirely**: 0.7296 / 0.7174 / 0.7706 vs fp32 0.7037 / 0.7014 / 0.7682 vs INT8 0.6615 / 0.6544 / 0.7019, at +5 % file size. Calibration-variance hypothesis rejected. Surprise: nanos end above fp32 purely through a +7-pt Child AP gain that int8 head convs produce (present in INT8 and D1, absent in `w8a32`/`w8a16`/D2) — unexplained, could be the adult→Child direction, LAGENDA sweep required. Prediction A was mis-designed (comb in a difference) and is recorded as failed; corrected test confirmed the grid (83 % vs 10 % control). Latency of D1 not measured (pod closed) | `experiments/EXP-2026-21-int8-output-quantization.md`, `docs/MODEL_COMPARISON.md` last section, `models/exp21_20260911/`, scripts `float_head_quant.py` `box_edge_hist.py` `box_grid_test.py` `tflite_tensor_inspect.py` `tflite_scopes.py` |
| 2026-09-11 | **EXP-2026-21 addendum** — latency bench of every INT8 variant, plus the full precision matrix (`.pt`, fp32 TFLite, INT8, D1 fix × 640/416/320) for `y26n_humanshaped_v2_distill_v1` (epoch 61/100 checkpoint) | D1 fix keeps INT8 speed (nano 22.9 vs 21.4 ms; y26s 46.6 vs 46.7); `w8a32` slower than fp32, `w8a16` 66× slower — both dead. Distill model reproduces the pattern on a 4th checkpoint: @640 fp32 0.6885 → INT8 0.6560 → D1 0.7105; @416 0.6778 / 0.6825 / 0.6928; @320 0.6482 / 0.6570 / 0.6842; trails `y26n_humanshaped_v2` by ~2 pts (unfinished run). Volume quota filled mid-run (silent `rc=120` kills) — freed 2.3 GB, volume enlarged, rerun | `experiments/EXP-2026-21-int8-output-quantization.md` (addendum), `docs/MODEL_COMPARISON.md`, `models/exp21_20260911/distill_pull/`, `vlm-cluster/distill_matrix.sh` |
| 2026-09-14 | **EXP-2026-22 — precision × resolution × dataset matrix incl. production** — 5 models × 5 precisions × 3 sizes on Spotlight-val and the `haramblur_holdout` QA set (150 cells) via a claim-queue over 1 GPU + 5 CPU pods; five pre-registered expectations | All five held (Child-gain one mostly): fp32-TFLite/FP16 parity ±0.004/±0.0003 everywhere; W8A8 INT8 costs 5–6 pts mAP50-95 on the holdout @640 for every YOLO26 model and the float-decode fix recovers it (±0.3 of `.pt`) at INT8 speed; production `yolo11N-640` trails every candidate on every collection (holdout @640 0.741 vs 0.828–0.870) and on person-free false blurs (7.0 % vs 4.3–5.1 %), and itself gains +2.2 from the fix; the Spotlight Child gain shrinks to ~+1 on the holdout `child` collection. Run cost ~4 h wall incl. two storage incidents (volume quota, then a resize that truncated in-flight sidecars) — both now caught by the runner's JSON/`n_images` guards. Latency measured uniformly afterwards on one CPU pod (`bench_matrix.py`, 60 files × {1,4} threads × 3 repeats): the fix is free over INT8, INT8 = 1.6–2.8× fp32, FP16 = fp32 speed, 640→416 ≈ 2.5× | `experiments/EXP-2026-22-precision-matrix.md`, `docs/MODEL_COMPARISON.md` last section, artifact 258b61fe, `models/exp22_20260914/`, scripts `matrix_lib.sh` `run_cells.sh` `join_pod.sh` `matrix_status.py` `build_matrix_report.py` |
| 2026-09-14 | **Threshold vs recall/precision across export precisions** — `y26n_humanshaped_v2` × {fp32 `.pt`, fp32 TFLite, FP16, INT8 W8A8, INT8 + float-decode fix} × {640, 416, 320} on `haramblur_holdout` (+ @640 on Spotlight-val), offline `pr_curve.py --grid-step 0.01 --agnostic` replay of the EXP-22 raw dumps (19 cells, every one n_images-asserted); requested as "a threshold-vs-recall/precision diagram across two quantizations to make sure quantization does not affect the numbers" | **At the shipped 0.45 it does not**: the fix export matches fp32 within 0.1 pt any-person recall and precision at every size (W8A8: −0.6…−1.0 recall, −0.3…−1.6 precision); Child precision is the exception (−4…−5 pts under both INT8 exports at 640, the precision-side view of the Child-AP gain). **But the threshold axis does not survive quantization**: INT8 confidences sit on a ~0.3-logit ladder with a hard ceiling — W8A8 0.860 / 0.572 / 0.500 (640/416/320), fix 0.726 / 0.707 / 0.694 — above which the export returns no boxes at all; in the fix export small/medium people (P3/P4) are clamped at exactly 0.500. Cause read off the files: per-tensor int8 class-logit convs with zero-point 124–127 (range set by −30…−75 background logits). mAP cannot see this. Gate for the INT8 export now includes floating the three class convs | `docs/MODEL_COMPARISON.md` → "Threshold vs recall/precision across export precisions — the INT8 confidence ceiling", `experiments/assets/pr_quant/*.svg` + `SUMMARY.md`, `models/pr_quant_20260914/`, pod `/workspace/exp22/pr_quant/`, memory `int8-confidence-ceiling` |
| 2026-09-15 | **TFLite latency on Apple silicon** — the EXP-22 `bench_matrix.py` method (now `--root`-capable) run on the owner's Mac (Apple M2, 4P+4E, LiteRT 2.2.0 + XNNPACK) for all 60 matrix files at 1/4/8 threads, compared cell-for-cell with the pod's EPYC bench | INT8 is worth far more on M-series than the pod showed: INT8-over-fp32 speed-up 2.8–4.0× on the M2 vs 1.6–3.0× on EPYC (an M2 P-core is 3.2× slower than an EPYC core on fp32 but only 1.8× on INT8). Float-decode fix = plain INT8 latency in all 15 cells. 4 threads is the sweet spot, 8 threads slower in 39/45 cells (E-cores). At 4 threads: nano INT8 @640 ≈ 16 ms, y26s INT8 @640 ≈ 37 ms, y26s @416 ≈ 13 ms (= nano @640 cost, +5 pts holdout mAP). Production is not faster than the YOLO26 nanos anywhere. CPU-only, not the extension's TF.js path; Mac not idle (40/180 measurements > 10 % spread, 1-thread rows stable) | `docs/MODEL_COMPARISON.md` → "TFLite latency on Apple silicon", `models/bench_mac_20260915/bench_mac_m2_all.json`, `experiments/assets/bench_mac/` (3 SVGs + SUMMARY.md), `experiments/make_charts_bench_mac.py` |
| 2026-09-15 | **Per-class AP vs confidence threshold** — the threshold sweep repeated with Woman/Man/Child AP50/AP50-95 as the metric (`vlm-cluster/ap_sweep.py`: `map_eval.py` matching once per class × IoU, threshold applied as a prefix; 17/17 cells crosschecked equal to the EXP-22 `_map.json` at t=0), same 19 cells as the recall/precision sweep | At 0.45 the float-decode export keeps every class within ~1 pt of fp32 AP50-95 (640: Woman −0.8, Man −0.1, Child +1.4); plain W8A8 is 6–7 pts down on Woman/Man. Above 0.45 the fix collapses per class (0.55: −4 Woman / −8 Man at 640) because P3/P4 people are clamped at 0.500; W8A8 is zero for every class at 0.70 @416 and 0.55 @320. The Child AP gain of the INT8 exports survives at the operating point (+1…+1.6 holdout, +6.4 Spotlight-val) even though Child precision at 0.45 is 4 pts lower — ranking vs fixed-cut, both true. Applying 0.45 alone costs every class 4–7 pts AP50-95 at fp32: ship decisions belong on the @0.45 columns, not the mAP tables | `docs/MODEL_COMPARISON.md` → "Per-class AP along the confidence threshold", `models/ap_sweep_20260915/`, `experiments/assets/ap_sweep/`, `experiments/make_charts_ap_sweep.py`, threshold-sweep artifact (last section) |

